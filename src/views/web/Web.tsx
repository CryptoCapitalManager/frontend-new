import { BrowserRouter } from "react-router-dom";

import Landing from "./componenets/Landing/Landing";

const Web = () => {
    return (
        <BrowserRouter>
            <Landing />
        </BrowserRouter>
    );
};

export default Web;
